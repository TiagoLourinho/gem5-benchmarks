unsigned short lfsr = 0xACE1u;
unsigned bit;

int main()
{

    int count = 0;

    for (int i = 0; i < 100000; i++)
    {

        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
        ////////// Basic Block //////////
        bit = ((lfsr >> 0) ^ (lfsr >> 2) ^ (lfsr >> 3) ^ (lfsr >> 5)) & 1;
        lfsr = (lfsr >> 1) | (bit << 15);

        if (lfsr % 2 != 0)
        {
            count++;
        }
        /////////////////////////////////
    }
}