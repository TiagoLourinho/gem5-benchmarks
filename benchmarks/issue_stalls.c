#include <math.h>
#include <stdlib.h>
#include <stdio.h>

int main() {
    int x = 1, y = 20, z = 10, k = 3;
    for (int i = 0; i< 30000; i++){
	    
        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

         y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

         y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

         y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

         y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);

        y = sqrt(x / z);
        x = sqrt(z/y);
        x = k / sqrt(z);

        x = k / sqrt(z);
        x = sqrt(z/y);
        y = sqrt(x / z);
       
    }

    return 0;
}
